# Your music stays local — never commit copyrighted audio.
audio/*
!audio/.gitkeep
_testaudio/
.DS_Store

# staged upload directory (built by deploy.sh)
.deploy/

# wrangler's local cache — holds your Cloudflare account id and email
.wrangler/
